import { ArrayOps } from '../lift';
declare module '../../wrapper' {
    interface ArrayOps<A> {
        take: typeof take;
    }
}
/**
 * Takes the first 'count' items from this Array.
 */
export declare function take<A>(this: ArrayOps<A>, count: number): ArrayOps<A>;
