export var iteratorSymbol = (typeof Symbol === 'function' && Symbol.iterator) || '@@iterator';
export function singleValueIterator(getValue) {
    return function () {
        var done = false;
        var value = getValue(this);
        return {
            next: function () {
                if (done)
                    return { done: true };
                done = true;
                return { value: value, done: false };
            }
        };
    };
}
