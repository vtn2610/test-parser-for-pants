import { ObjectOps, ArrayOps } from '../lift';
declare module '../../wrapper' {
    interface ObjectOps<A> {
        values: typeof values;
    }
}
/**
 * Creates an Array of all this object's values.
 */
export declare function values<K extends string, V, W>(this: ObjectOps<Record<K, V>>): ArrayOps<V>;
