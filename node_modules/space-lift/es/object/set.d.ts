import { ObjectOps } from '../lift';
/**
 * Creates a Set-like object (string keys, true values) from a list of keys
 */
export declare function Set<K extends string>(...keys: K[]): ObjectOps<Record<K, true | undefined>>;
