import { ObjectOps } from '../lift';
declare module '../../wrapper' {
    interface ObjectOps<A> {
        filter: typeof filter;
    }
}
/**
 * Filter keys/values of a "Map" object
 */
export declare function filter<K extends string, V>(this: ObjectOps<Record<K, V>>, predicate: (key: K, value: V) => boolean): ObjectOps<Record<K, V>>;
