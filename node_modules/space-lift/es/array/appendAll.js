import { ArrayOps } from '../lift';
/**
 * Appends an Array of items at the end of the Array.
 */
export function appendAll(items) {
    return new ArrayOps(this.value().concat(items));
}
ArrayOps.prototype.appendAll = appendAll;
