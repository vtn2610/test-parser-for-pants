import { ArrayOps } from '../lift';
/**
 * Drops the last 'count' items from this Array.
 */
export function dropRight(count) {
    return new ArrayOps(this.value().slice(0, -count));
}
ArrayOps.prototype.dropRight = dropRight;
