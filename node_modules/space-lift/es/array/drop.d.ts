import { ArrayOps } from '../lift';
declare module '../../wrapper' {
    interface ArrayOps<A> {
        drop: typeof drop;
    }
}
/**
 * Drops the first 'count' items from this Array.
 */
export declare function drop<A>(this: ArrayOps<A>, count: number): ArrayOps<A>;
