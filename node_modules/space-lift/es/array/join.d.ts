import { ArrayOps, StringOps } from '../lift';
declare module '../../wrapper' {
    interface ArrayOps<A> {
        join: typeof join;
    }
}
/**
 * Joins the items into a string, using a separator.
 */
export declare function join<A>(this: ArrayOps<A>, separator?: string): StringOps;
